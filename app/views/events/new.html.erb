<div class="col-md-12">
          <h1>Criar evento da ONG</h1>
  <div class="box">
    <div class="container-fluid">
      <div class="row">
        <%= simple_form_for [@ong, @event] do |f| %>
          <%= f.input :name, label: 'Nome' %>
          <%= f.input :address, label: 'Endereço' %>
          <%= f.input :datetime, label: 'Date e Hora' %>
          <%= f.button :submit, 'Enviar' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
